dependencies:
  cache_directories:
    - ~/.android
  pre:
    - >
      yes |
      android update sdk --all --no-ui
      --filter android-23,tools,platform-tools,build-tools-23.0.2,extra-android-support,extra-android-m2repository

